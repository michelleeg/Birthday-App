var birthdayButton = document.getElementById();

function validDate(){
    var date = documement.getElementById('inputDate').value;
    var invalid = "Sorry, but you did not enter a date in the correct format.";
    var check1 = str.substring(2,3)
    var check2 = str.substring(5,6)
    if (check1 =="/" && check2 == "/"){
        return ('calculateAll.').value

    }else {
        return(invalid);
    }
    if (new Date(date).getTime == true || date.lemgth > 10 || date.length <8) {
        document.getElementById('errorText').textcontent = sorry;
    }
}
button.addEventListener('click', invalid);
